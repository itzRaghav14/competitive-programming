#include <bits/stdc++.h>
using namespace std;

#ifndef itzzRaghav
#define dbg(...) ;
#define debug(...) ;
#define crndl ;
#endif

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  auto solve = [&]() {

  };

  int t;
  cin >> t;

  for (int tc = 1; tc <= t; tc++) {
    debug(Testcase, tc);
    solve();
    crndl;
  }

  return 0;
}