#include <bits/stdc++.h>
using namespace std;

#ifndef itzzRaghav
#define dbg(...) ;
#define debug(...) ;
#define crndl ;
#endif

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  auto solve = [&]() {
    
  };

  int t = 1;
  cin >> t;

  for (int tc = 1; tc <= t; tc++) {
    debug(Testcase, tc);
    solve();
    crndl;
  }

  return 0;
}